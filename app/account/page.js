import Navigation from "../_components/navigation";

export const metadata = {
  title: "Guest Area",
};

const Page = () => {
  return (
    <div>
      <h1>Creating new Account here</h1>
    </div>
  );
};

export default Page;
